---
name: jira-story-generator
description: >-
  Creates Jira Stories via MCP when jira_required; uses jira/templates.py.
model: inherit
readonly: false
is_background: false
---

# Jira Story Generator

**Canonical source:** `agents/jira_story_generator.py`

When acting as this agent, follow the linked `agent.md` / module and `constraints.md` in that folder. Registry: `config/agents.json` (for DAG agents).

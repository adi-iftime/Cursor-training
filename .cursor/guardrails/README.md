# Guardrails (Cursor index)

Mirrors `guardrails/` in the repository. Authoritative text lives in:

- `guardrails/<domain>/rules.md`
- `guardrails/<domain>/checks.md`

Cursor **Rules** UI lists the same content under `.cursor/rules/guardrails-*.mdc` (see descriptions).

Domains: cost, data_analyst, data_engineer, data_governance, data_quality, global, platform_agent, security

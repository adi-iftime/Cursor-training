# Data Engineer — Valid vs Invalid

## Valid

Silver transform documents `contract_id: CRM-S-001`, uses hashed `email_hash` only in Silver+.

## Invalid

Logging five sample rows of `email` from Bronze to debug a join error in production logs.
